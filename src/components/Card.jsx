// import '../styles/Card';

function Card() {
    return (
        <div>
            <div className='Header'>${}</div>
            <div className='body'>${}</div>
        </div>

    );
}

export default Card();